package org.wrpg.mnes.tests;

/**
 * http://www.qmtpro.com/~nes/misc/
 * 开发 CPU 时，先将 PC 设置为 0xC000，
 * 再让 CPU 运行，每运行一条指令后，
 * 和它的 nestestlog.txt 对照一下各个寄存器状态，
 * 这样能按照它的 log 顺序逐条开发出正确的指令，
 * 比一口气开发完再去 debug 好得多
 *
 */
public class CPUTests {



}
